<div class="container">
  <div class="row">
    <div class="col-md-3">
      <h4 class="filter-header">Tipo de transacción <span>Selecciona para filtrar</span></h4>
      <div class="list-group filter-type">
        <% @expense_types.each do |expense_type| %>
          <%= link_to "#{expense_type}", filter_expense_type_path, remote: true, class: 'list-group-item' %>
        <% end %>
      </div>

      <h4 class="filter-header">Categoría <span>Selecciona para filtrar</span></h4>
      <div class="list-group filter-type">
        <% @categories.each do |category| %>
          <%= link_to "#{category}", filter_category_path, remote: true, class: 'list-group-item' %>
        <% end %>
      </div>
    </div>
    <div class="col-md-9 index-data">
      <div class="row" style="margin-top: 10px;">
        <div class="summary-box col-md-4 text-center">
          <div class="summary-value">$2,500,000</div>
          <div class="summary-label">Total</div>
        </diV>
        <div class="summary-box col-md-4 text-center">
          <div class="summary-value">2</div>
          <div class="summary-label">Transactions</div>
        </div>
        <div class="summary-box col-md-4 text-center">
          <div class="summary-value">$1,000,000</div>
          <div class="summary-label">Average</div>
        </div>
      </div>

      <div class="row" style="margin-top: 20px;">
        <div class="col-md-12">
          <%= link_to new_expense_path, remote: true, class: 'btn btn-success pull-left' do %>
            <span class="glyphicon glyphicon-plus"></span>New Expense
          <% end %>
          <h4 class="dropdown pull-right">
            <button type="button" class="dropdown-toggle btn btn-link" data-toggle="dropdown"><%= "#{Date::MONTHNAMES[@time_now.month]} #{@time_now.year}" %><span class="caret"></span></button>
            <ul class="dropdown-menu" role="menu">
              <% (1..12).each do |i| %>
                <li role="presentation"><a href="#"><%="#{Date::MONTHNAMES[(@time_now - i.month).month]} #{@time_now.year}" %></a></li>  
              <% end %>
            </ul>
          </h4>
        </div>
      </div>

      <table id="expenses-table" class="table table-hover" style="margin-top: 20px;">
        <thead>
          <tr>
            <th class="text-center">Día</th>
            <th>Concepto</th>
            <th class="text-right">Categoría</th>
            <th class="text-right">Valor</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td class="date">
              <div class="day">24</div>
              <div class="month">Apr</div>
            </td>
            <td>
              <div>
                <span class="type">Purchase</span>
              </div>
              <div>
                <span class="concept">Example concept</span>
              </div>
            </td>
            <td class="category text-right">Restauratns</td>
            <td class="text-right" style="width: 170px;">
              <div class="amount">$1,000,000</div>
              <div class="actions">
                <a href="#"><span class="glyphicon glyphicon-edit"></span> Edit</a>
                <a href="#"><span class="glyphicon glyphicon-trash"></span> Delete</a>
              </div>
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</div>
